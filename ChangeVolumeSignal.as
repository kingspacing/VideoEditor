package com.editor.controller.signals
{
	import org.osflash.signals.Signal;
	
	public class ChangeVolumeSignal extends Signal
	{
		public function ChangeVolumeSignal(...valueClasses)
		{
			super(valueClasses);
		}
	}
}
